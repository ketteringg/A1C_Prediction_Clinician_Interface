# Copy to .env.local for local dev. NEVER commit .env.local.
# In Vercel, set these under Project Settings → Environment Variables.
# These are SERVER-SIDE only — never sent to the browser.
AUTH_USER=demo
AUTH_PASS=changeme
